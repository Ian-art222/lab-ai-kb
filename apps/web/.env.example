# Leave empty in local development to use the built-in Vite proxy:
#   /api -> http://127.0.0.1:8000
# Set this only when the frontend must call a different backend origin directly.
VITE_API_BASE_URL=
